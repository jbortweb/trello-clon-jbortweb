import React from 'react';
import { useState } from 'react';

const Form = () => {
    const [openForm,setOpenForm] = state(falso);

  return (
    <div>
        <button></button>
    </div>
  )
}

export default Form